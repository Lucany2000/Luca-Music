import { StyleSheet, StatusBar, SafeAreaView, Button, FlatList, ScrollView, View, useWindowDimensions } from 'react-native';

const Artist = () => (
    <View style={{ flex: 1, backgroundColor: '#673ab7' }} />
  );

export { Artist };  